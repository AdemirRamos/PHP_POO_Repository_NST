<?php

    //Aula 21 - Variáveis de Ambiente e Servidor Embutido

    echo 'Olá, mundo!';

    //O comando acima (ou quaisquer outros que, eventualmente, sejam escritos) pode ser visualizado através do "prompt" de comando.

    //Dicas:

    //Para rodar um arquivo no "prompt" de comando, usamos o comando "php (nome do arquivo sem parênteses)".
    //Para iniciar o servidor embutido do PHP através do "prompt" de comando, usamos o comando: "php -S localhost:(número da porta);
    //O número da porta é facultativo. Recomanda-se a porta "80"; caso essa esteja ocupada, se pode usar "8080".

?>
